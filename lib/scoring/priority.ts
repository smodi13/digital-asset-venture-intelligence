/**
 * Priority.
 *
 * The PriorityState enum exists, but Phase 5B activates NO automatic
 * ESCALATE / MONITOR / PASS decision function. Priority calibration must
 * follow observation of the real 39-company analytical distributions, which is
 * a later phase. There is deliberately no production decision function here.
 */

export const PRIORITY_THRESHOLDS_ACTIVE = false;
